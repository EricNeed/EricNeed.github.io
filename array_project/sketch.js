// Project Title
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"



function setup() {
  createCanvas(windowWidth, windowHeight, WEBGL);
}

function draw() {
  background(220);

  for(let c = 1; c<character_list.length; c++){//the first index is the current id

  }

  orbitControl();

  plane(10000, 10000);

  translate(0, 0, 0);
  box(100, 100);
}